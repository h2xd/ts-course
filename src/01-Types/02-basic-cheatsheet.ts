// 01: Core three
const truthOrDare: boolean = true
const isActive = false

const oneNumberToRuleThemAll: number = 42
const symState = 7
const price = 1.337

const bibelQuote: string = 'Training for the ballet, Potter?'
const template = `
  ${price} of the bag i just bought
`

// 02: Specials
const pleaseHoldTheLine = null

const whatIsLife = undefined

const anarchyData: any = {
  age: 27,
  mass: 'critical',
  hasFoundTheAnswer: false
}

const superSpecialCase: unknown = null

function openTheDoor(): void {
  console.log('Door is open!')
}

// 03: Parameter
function killTheBug(withForce: true) {
  return 'It shall burn in hell!'
}
